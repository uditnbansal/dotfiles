[color]
  ui = true
[core]
  editor = vim
  pager = less -F -X
[alias]
  a = add
  b = branch
  bi = bisect
  bl = blame
  bn = rev-parse --abbrev-ref HEAD
  c = checkout
  ch = checkout --force HEAD
  cm = checkout master
  co = commit
  cp = cherry-pick
  ctag = describe --tags --abbrev=0
  d = diff
  dn = diff --name-only
  l = log
  ll = log --oneline
  ln = log --oneline -n
  lo = log
  pi = push origin HEAD:refs/for/ipc
  pg = push origin HEAD:refs/for/master
  pl = pull
  ph = push
  reb = rebase
  rebm = rebase master
  res = reset
  s = show --stat
  sh = show
  sn = show --pretty="format:" --name-only
  sp = stash show -p
  st = status
[user]
  name = Udit Bansal
  email = udit@tower-research.com
[push]
  default = simple
[credential]
	helper = store
